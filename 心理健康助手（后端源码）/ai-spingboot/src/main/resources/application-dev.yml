# 开发环境专用配置
# 注意：此文件包含敏感密钥，禁止提交到公开仓库！

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mental_health_assistant?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:请填写数据库密码}

rag:
  embedding:
    api-key: ${EMBEDDING_API_KEY:请填写Embedding API密钥}

logging:
  level:
    org.example.aispingboot.AiService: DEBUG
